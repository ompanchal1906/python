#4.	Create a list of tuples containing a food item and its price. Sort the tuples in descending order by price.
# (Hint: use sorted() function with lambda)
a=[('apple',50),('banana',30),('orange',40),('grapes',60)]
b=[]
print(a[0][0])
for i in a:
    b.append(a.sorted())
print(b)